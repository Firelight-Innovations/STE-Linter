# T1 rule exceptions (preceding-word gate)

A T1 rule that carries an `exceptions` list stays silent when the flagged
word sits in a fixed compound, and still fires on the plain replaceable use.
The gate reads the one word in front of the match. Line numbers below are
asserted by tests/run_suggestion_tests.py, and the runner reflows nothing.
Never linted in a normal run, because tests/corpus_suggestions/ sits in
never_lint.

Open a pull request against the release branch.
The merge request lists 4 files.
The API request returned 4 rows.
Operators request a new key each year.
The interface holds 4 buttons.
Vendors interface with the crew each week.
