<!-- lint-profile: core -->
# Dirty Terminology

The oldwidget still shows up in the old code `expect:VEI-S7-TERM-0001`.

The futuremodule ships next quarter `expect:VEI-S7-TERM-0001`.

Use oldname instead of the real name `expect:VEI-T1-TERM-0001`.

The XYZQ system needs a review `expect:VEI-S7-ABBR-0001`.
