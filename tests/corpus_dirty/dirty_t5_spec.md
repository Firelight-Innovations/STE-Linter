<!-- lint-profile: spec -->
# Dirty T5 Spec

The renderer shall draw the frame and the audio mixer shall play the cue `expect:STE-T5-MULTI-0001`.

The build takes about 10 minutes on a clean machine `expect:STE-T5-NOSHAL-0001`.

Users shall receive a confirmation email `expect:STE-T5-EARS-0001` `expect:STE-S7-ARTICLE-0001`.
