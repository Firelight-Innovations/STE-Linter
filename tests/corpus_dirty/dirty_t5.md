<!-- lint-profile: core -->
# Dirty T5

Save and/or load the file `expect:STE-T5-ANDOR-0001`. Configure the input/output device `expect:STE-T5-SLASH-0001`.

The system shall log the id, name, time, place, and reason `expect:STE-T5-PUNC-0001`.

This single sentence deliberately runs on for far too many words in order to cross the twenty word budget line for the core profile easily `expect:STE-BUD-0001`.
