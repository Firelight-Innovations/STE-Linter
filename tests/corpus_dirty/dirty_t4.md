<!-- lint-profile: core -->
# Dirty T4

This breaks the build `expect:STE-T4-PRO-0022`. The new loader is faster `expect:STE-T4-COMP-0003`.

The install runs quicker than expected `expect:STE-T4-COMP-9999`.
